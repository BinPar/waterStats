import waterAccumulation from './waterAccumulation';

const testArray = [4, 2, 1, 3, 0, 1, 2];
const result = waterAccumulation(testArray);

// eslint-disable-next-line no-console
console.log(`The result is: ${result}`);
